###############################################
# These are more abstract use scenario tests. #
###############################################

describe "TuringEmailApp", ->

  it "should allow a user to login", ->
    return

  it "should allow a user to send an email", ->
    return

  it "should allow a user to create a new draft", ->
    return

  it "should allow a user to update settings", ->
    return

  it "should allow a user to delete an email", ->
    return

  it "should allow a user to create a new email draft", ->
    return

  it "should allow a user to run the brain by clicking a button", ->
    return
